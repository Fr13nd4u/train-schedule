import React from "react";
import styled from "styled-components";

export const ScheduleItem: React.FC = () => {
  return <ScheduleItemWrap>ScheduleItem</ScheduleItemWrap>;
};

const ScheduleItemWrap = styled.li``;
